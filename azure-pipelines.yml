trigger:
- main

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  imageName: 'g3api'

steps:
- task: Docker@2
  displayName: Build an image
  inputs:
    repository: $(imageName)
    command: build
    Dockerfile: SpacePort/Dockerfile
